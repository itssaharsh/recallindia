# Matcher Lambdas (CodeUri backend/matcher/). rapidfuzz is used only by textmatch.py (P04);
# never import it from backend/common. jinja2 + reportlab render the claim letter (claim.py, P09).
rapidfuzz>=3,<4
jinja2>=3.1,<4
reportlab>=4,<5
