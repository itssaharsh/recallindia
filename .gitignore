# secrets
.env
.env.*
!.env.example

# Windows / WSL metadata
*:Zone.Identifier
*Zone.Identifier

# python
__pycache__/
*.pyc
.venv/
venv/

# node
node_modules/
.next/

# sam
.aws-sam/

# local demo store (DEMO_MODE stand-in for DynamoDB + S3)
.demo_store/
# scripts/backfill.py resumable cursors
.backfill/

# tool caches
.pytest_cache/
.ruff_cache/

# note: .secrets.baseline stays tracked (pre-commit detect-secrets reads it)

# the build prompts and the design pack they arrive in: inputs to the work, not the work
recallindia-prompts/
*.zip

# the v3 pack ships its components as a second copy of app/src/components/v3; only the app's copy
# is the source. The spec, the tokens, the brand files and the mockups stay under docs/v3.
docs/v3/code/

# the static export
app/out/
*.Zone.Identifier
